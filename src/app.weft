// app.weft

import WPRepository
import MainView

@Main
class ClearlyReformed: App {
  @State var wpRepo = WPRepository() // singleton instantiation

  var content: View = {
    MainView() {
      environment: [wpRepo]
  }
}
